[package]
name = "scan-repair-local"
version = "0.1.8"
description = "Private scan repair and OCR"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
