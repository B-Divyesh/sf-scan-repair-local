[package]
name = "scan-repair-local"
version = "0.1.0"
description = "Private scan repair and OCR"
edition = "2021"

[lib]
name = "scan_repair_local_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
